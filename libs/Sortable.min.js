<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>SortableJS Example</title>
  <!-- Include SortableJS from CDN -->
  <script src="https://cdn.jsdelivr.net/npm/sortablejs@1.15.0/Sortable.min.js"></script>
  <style>
    #sortable-list { list-style: none; padding: 0; width: 200px; }
    #sortable-list li { margin: 5px; padding: 10px; background-color: #f0f0f0; border: 1px solid #ccc; cursor: grab; }
  </style>
</head>
<body>

  <ul id="sortable-list">
    <li>Item 1</li>
    <li>Item 2</li>
    <li>Item 3</li>
    <li>Item 4</li>
  </ul>

  <script>
    // Initialize SortableJS
    const sortable = new Sortable(document.getElementById('sortable-list'), {
      animation: 150,       // Smooth animation when dragging
      ghostClass: 'sortable-ghost', // Class for the placeholder when dragging
      onEnd: function (evt) {
        console.log(`Item moved from index ${evt.oldIndex} to ${evt.newIndex}`);
      }
    });
  </script>

</body>
</html>
